#include<stdio.h>
int num(int);
int main()
{
    int n,s;
    printf("Enter the value of n is ");
    scanf("%d",&n);
    printf("factorial is ");
    s=num(n);
    printf("%d",s);
    return 0;

}
int num(int a)
{
    int i,fact=1;
    for(i=1;i<=a;i++)
        fact=fact*i;
    return fact;
}
